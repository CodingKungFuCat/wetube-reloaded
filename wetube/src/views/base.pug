doctype html 
html(lang="ko")
    head
        link(rel="stylesheet" href="https://unpkg.com/mvp.css@1.12/mvp.css")
        title #{pageTitle} | wetube
    body 
       main 
        header 
        if fakeUsers.loggedIn 
            small hello #{fakeUsers.username}
            nav
                ul  
                if fakeUsers.loggedIn
                    li
                        a(href="/logout")    Log Out 
                else
                    li
                        a(href="/login")    Login              
            h1=pageTitle
      
                block content
                br
                include partials/footer.pug