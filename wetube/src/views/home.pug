extends base.pug
include mixins/video


block content 
        h3 welcome you will see trending video

        ul 
        each potato in videos
             +video(potato)
        else 
                li 죄송합니다. 데이터를 불러올 수 없습니다.

                //- 위의 예시처럼 데이터를 불러들일 수 있는 partial을 mixins 라고 한다. 